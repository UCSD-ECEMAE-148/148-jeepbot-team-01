# Troubleshooting

## Camera not detected
- Check USB cable
- Reconnect camera
- Try another USB port
- Confirm the Python environment is active
- Verify `depthai` is installed

## Depth not showing
- Make sure the stereo pipeline is enabled in code
- Some camera examples only show RGB unless depth is explicitly configured
